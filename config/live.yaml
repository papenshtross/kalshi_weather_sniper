name: example-mm-live
mode: live

instruments:
  - symbol: trump-wins-2028-YES
    token_id: "123..."

strategy:
  class: polybot.strategies.example_mm:ExampleMM
  params:
    instrument_id: "trump-wins-2028-YES.POLYMARKET"
    half_spread_bps: 120        # wider when real money
    order_size: "10"
    max_inventory: "50"
    refresh_secs: 5
